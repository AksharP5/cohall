interface:
  display_name: "Cohall"
  short_description: "Delegate work across your Cohall devices"
  default_prompt: "Use $cohall to delegate this task to the device with the right local tools and return the result."
