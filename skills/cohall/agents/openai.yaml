interface:
  display_name: "Cohall"
  short_description: "Delegate to your devices and named bots"
  default_prompt: "Use $cohall to choose the right device or named bot, send this task with relevant conversation context, and return the result."
